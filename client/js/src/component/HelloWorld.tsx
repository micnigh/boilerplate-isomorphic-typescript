let [first, , last] = [1, 2, 3];
console.log(`if you ain't ${first} your ${last}`);
console.log("HelloWorld Imported");

export interface WhereDoesThisGo {
  isItDeleted: boolean;
  isItInSourceMaps: boolean;
}

export default "Dude";
